<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:LazyFit.Views"
             xmlns:psViews="clr-namespace:LazyFit.Views.Pressure"
             xmlns:fastViews="clr-namespace:LazyFit.Views.Fasting"
             xmlns:reportViews="clr-namespace:LazyFit.Views.Reports"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="LazyFit.MainPage"
             Shell.NavBarIsVisible="False"
             Background="{StaticResource White}">

    <Page.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{StaticResource White}" StatusBarStyle="DarkContent"/>
    </Page.Behaviors>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid ZIndex="100" Grid.RowSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource LazyColor}" Margin="25" Padding="25">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="7"/>
                </Border.StrokeShape>

                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding OpenLogButtonsCommand}"/>
                </Border.GestureRecognizers>


                <Label Text="Log" 
                       TextColor="White"
                       FontSize="Body"
                       HorizontalOptions="Center" 
                       VerticalOptions="Center"/>

            </Border>

        </Grid>

        <StackLayout Grid.Row="0" Orientation="Vertical" Padding="0,5">
            <StackLayout Orientation="Horizontal">
                <Label Text="LazyFit" Style="{StaticResource PageHeader}" HorizontalOptions="StartAndExpand"/>
                <ImageButton Source="pie_chart.png" WidthRequest="30" Margin="0,0,10,0" HorizontalOptions="End" Command="{Binding OpenResults}"/>
            </StackLayout>
            
            <fastViews:ActiveFast/>
            
        </StackLayout>
        

        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Spacing="10" Padding="0">

               <Border StrokeThickness="0" Padding="0">
                    <reportViews:MainQuickView/>
                </Border>
                       
                <!--<Border>
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="7"/>
                    </Border.StrokeShape>
                    <views:LogButtonsView />
                </Border>-->

                <psViews:PressureCardLineView/>
                
                <views:LatestActionsView/>
                
            </StackLayout>
            
            
        </ScrollView>


        <Label Grid.Row="3" Text="{Binding AppVersion}" HorizontalTextAlignment="Center" Margin="5"/>

    </Grid>

</ContentPage>
