<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:LazyFit.Views"
             x:Class="LazyFit.ResultsPage"
             Title="ResultsPage"
             Background="{StaticResource GrayBackground}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

        <StackLayout Orientation="Vertical">
            
            <Label Text="Weekly results"
                   HorizontalTextAlignment="Center"
                   Style="{StaticResource PageHeader}"/>

            <Label Text="{Binding PeriodText}" 
                   HorizontalTextAlignment="Center"
                   Style="{StaticResource PageSubHeader}"/>
            
        </StackLayout>

        <ScrollView Grid.Row="1">

                <VerticalStackLayout Spacing="10" Padding="10,0">
                    
                <Border StrokeThickness="1" Padding="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="5" />
                    </Border.StrokeShape>
                    
                    <views:WeightResultChart />
                </Border>

                <Border StrokeThickness="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="7" />
                    </Border.StrokeShape>
                    <views:FastChart/>
                </Border>

            </VerticalStackLayout>
            
            </ScrollView>

        <!-- Controls -->
            <Grid Grid.Row="2" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>


            <Button Grid.Row="0" Grid.Column="0" Text="&#x2190;" FontSize="36" Command="{Binding ShowOlder}" Style="{StaticResource TextButton}" Background="White" />
            <Button Grid.Row="0" Grid.Column="1" Text="&#x2192;" FontSize="36" Command="{Binding ShowNewer}" Style="{StaticResource TextButton}" Background="White" />


        </Grid>

    </Grid>

</ContentPage>